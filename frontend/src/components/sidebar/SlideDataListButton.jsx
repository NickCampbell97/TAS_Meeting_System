import React from 'react';
import { ListItemButton } from '@mui/material';

/*
const SlideDataListButton = ({ onDoubleClick }) => {
    return (
        <ListItemButton key={index} dense divider onDoubleClick={onDoubleClick}> 
        </ListItemButton>
    );
};
*/

const SlideDataListButton = ({ onDoubleClick }) => {
    return (
        <ListItemButton dense divider onDoubleClick={onDoubleClick}>
        </ListItemButton>
    );
};

export default SlideDataListButton;
